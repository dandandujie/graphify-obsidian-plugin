{
  "name": "graphify-obsidian-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian plugin for Graphify (Claude Code + Codex bridge)",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.17.57",
    "obsidian": "^1.8.9",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9"
  }
}
